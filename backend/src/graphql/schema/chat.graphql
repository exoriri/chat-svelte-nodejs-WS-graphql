type Message {
    id: ID!,
    user_id: ID!,
    content: String!
}

type Chat {
    id: ID!,
    user_id: ID!, 
    messages: [Message]
}

type ChatsResult {
    chats: [Chat]
}

union ChatsMessagesResult = ChatsResult | Error

type Query {
    chatMessages(chat_id: ID!): ChatsMessagesResult
}